{
  "name": "webmcp-next-monorepo",
  "private": true,
  "packageManager": "pnpm@10.31.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter demo-app dev",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  }
}
